import { MessageAutoManagement } from "@/components/messageauto/MessageAutoManagement";

export default function MessageAutoPage() {
  return <MessageAutoManagement />;
}

export const metadata = {
  title: "ຂໍ້ຄວາມອັດໂນມັດ",
};
